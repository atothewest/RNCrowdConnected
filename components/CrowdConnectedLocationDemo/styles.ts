import { StyleSheet } from "react-native";

export default StyleSheet.create({
  output: {
    marginVertical: 12,
  },
  wrapper: {
    alignItems: 'center',
    display: 'flex',
    height: '100%',
    justifyContent: 'center',
  },
});
